
Cloud
Claude finished the response
Uploaded image
It ran successfully and I can see the peter-evans step executed (it's mentioned in the warning). But still no PR, which points to the pull-request step finding "no changes" — meaning Claude's edit isn't surviving into the step that commits. Let me confirm that's what happened.

Click the change job (green box). You'll see the steps listed. Find and expand the "Open pull request" step (click on it), then scroll to the bottom and screenshot it.

I'm looking for a line like "No changes detected" or similar. If that's what it says, I know the exact fix — Claude's file edits aren't carrying into the commit step, and I'll switch the workflow to have Claude commit its own work directly (which we now know it can do, since the app is installed). One screenshot of that step and I'll nail it down.

Uploaded image
That step ran cleanly but quietly skipped making a PR — which means when it checked, there were no file changes to commit. So Claude's edit isn't surviving from the "Run Claude" step into the commit step. I need to see what Claude itself reported to pick the right fix.

Please expand the Run Claude step this time (click on it, line ~3 in the list), and scroll to the very bottom of its output. Screenshot that bottom part — I want to see Claude's final summary (did it say it edited index.html, or that it couldn't, or something else?).

That single screenshot tells me whether the fix is "make Claude commit its own work" or "point the PR step at where the action put the change" — and then I'll give you the final workflow tweak. We're genuinely close; this is just the action behaving differently than the standard pattern, and I want to correct it precisely rather than guess.

Uploaded image
Almost — that's the top of the step (the setup output). I need the bottom of it, where Claude reports what it did. Keep scrolling down within that "Run Claude" section — past all the setup lines — until you reach the end of it (just before the "Post Run Claude" step begins).

Screenshot that bottom portion. I'm looking for Claude's final message — something like "I've added the comment to index.html" or a note that it made no changes. That's the one piece that tells me whether Claude actually edited the file, and it's the last thing I need to give you the definitive fix.

Uploaded image
That bottom bit (a transfer meter reading all zeros) tells me Claude was fussing with network/git calls at the end rather than cleanly leaving an edited file — which is exactly why the standard pattern isn't catching a change. I've got enough to stop guessing. Let me switch the workflow to a direct, self-diagnosing version: Claude only edits files, then a plain script commits whatever changed and opens the PR itself — and if nothing changed, it prints that in plain text so we'd see it immediately.

This is the last workflow swap. Same routine as before — and I know the copy-paste is tedious, thank you for hanging in.

Go to Code → .github/workflows → claude-box.yml, click the pencil ✏️, and make sure you're in the editor (not read-only view).
Ctrl+A, Delete to fully clear it.
Copy from name: Change Box down to the last line, and paste:
name: Change Box
run-name: "Box · ${{ inputs.ticket }} · ${{ inputs.request }}"

on:
  workflow_dispatch:
    inputs:
      request:
        description: "What to change"
        required: true
        type: string
      ticket:
        description: "Tracking id (set automatically by the app)"
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

concurrency:
  group: change-box
  cancel-in-progress: false

jobs:
  change:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Claude
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_args: "--max-turns 25"
          prompt: |
            Edit the files in this repository to satisfy the request below.
            Only edit files. Do NOT run git, do NOT commit, and do NOT open a
            pull request — another step handles that. Just make the file edits
            and stop.

            Request: ${{ inputs.request }}

      - name: Commit and open pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REQUEST: ${{ inputs.request }}
          TICKET: ${{ inputs.ticket }}
        run: |
          git config user.name "Change Box"
          git config user.email "actions@users.noreply.github.com"
          echo "----- changed files -----"
          git status --porcelain
          if [ -z "$(git status --porcelain)" ]; then
            echo "No file changes were made — nothing to open a PR for."
            exit 0
          fi
          BRANCH="box/${TICKET}"
          git checkout -b "$BRANCH"
          git add -A
          git commit -m "[${TICKET}] ${REQUEST}"
          git push -u origin "$BRANCH"
          gh pr create --base main --head "$BRANCH" --title "[${TICKET}] ${REQUEST}" --body "Automated change from the Change Box app. Request: ${REQUEST} (ticket ${TICKET})"
Confirm line 1 is name: Change Box, then Commit changes… → commit directly to main.
Then run a fresh test from Actions → Change Box → Run workflow with the same request and tracking id box-test-003.

This time, whatever happens, the log will tell us plainly: either a PR appears, or the "Commit and open pull request" step prints "No file changes were made" — which would prove Claude's edits aren't landing on disk and point us to the final adjustment. Tell me "done" once it's committed and running.







Claude is AI and can make mistakes. Please double-check responses.

claude-box.zip
Connectors

Web Search
